# In `subprojects` directory, ignore everything except .wrap files.
/subprojects/*
!/subprojects/*.wrap

# By convention, all directories for build artifacts start with `_build`.
# Ignore all such directories.
/_build*/

# The autogenerated configuration file for doxygen.
/doc/libssi/doxyfile
